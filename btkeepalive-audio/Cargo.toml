[package]
name = "btkeepalive-audio"
version = "0.1.0"
description = "Audio rendering for BT KeepAlive: precomputed tables, lock-free model, WAV export"
edition.workspace = true
license.workspace = true

[dependencies]
btkeepalive-core = { path = "../btkeepalive-core" }
rand = "0.9"

[target.'cfg(target_os = "windows")'.dependencies]
cpal = "0.15"

[dev-dependencies]
tempfile = "3"
